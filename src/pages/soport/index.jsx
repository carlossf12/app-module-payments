import React, { useState } from "react";
import Card from "@/components/ui/Card";

const Chat = () => {
    return (
        <div>
            <Card title="Formas de pago">Chat</Card>
        </div>
    );
};

export default Chat;
